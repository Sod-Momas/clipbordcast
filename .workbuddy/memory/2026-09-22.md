# 2026-09-22

- 用户不想本地装 Rust 环境 → 改为 GitHub Actions 在线编译（提交 4dfd8cc）。
- 新增 `.github/workflows/build.yml`：
  - build-windows：windows-latest（自带 Rust/MSVC/WebView2），npm install + npm run build，产物 clipbordcast.exe 上传 artifact。
  - build-android：ubuntu-latest + temurin JDK17 + gradle/actions/setup-gradle（gradle 8.9，仓库无 wrapper），assembleDebug，产物 app-debug.apk 上传 artifact。
  - 触发：push main + workflow_dispatch。
- 本机环境备忘：无 Rust、无 MSVC、无 Android SDK，Java 仅 JDK 8（不够用）；无 gh CLI。
- 待办：用户需在 GitHub 建私有仓库并 push（未配置远程）；产物未签名（exe 有 SmartScreen 提示、debug APK 可直接装）。
- 远程已配置 git@github.com:Sod-Momas/clipbordcast.git；本沙箱无 SSH key，push 需用户自己执行（用户已有可用的推送方式，CI 已跑过首轮）。
- 首次 CI 问题修复（提交 3a840ab）：actions 全部升级 v5（Node20 弃用警告）；tauri-build 在 Windows 强制需要 src-tauri/icons/icon.ico，用 Pillow 脚本 gen_icon.py 生成（蓝底圆角+白色剪贴板图形，16~256 多尺寸 ico + png）。
