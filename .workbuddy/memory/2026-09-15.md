# 2026-09-15

- 启动新项目 ClipBordcast：多平台剪贴板监听广播应用（手机验证码→PC、PC URL→平板、淘口令→手机）。
- 完成技术选型调研文档 `docs/2026-09-15_剪贴板广播技术选型调研.md`：
  - 核心难点：Android 10+ 后台读剪贴板被封（候选：无障碍服务 / IME / 通知栏监听），MIUI 保活。
  - 传输层推荐：LAN（mDNS+TLS WebSocket）优先，公网中继（WS/MQTT）兜底，BLE v1 砍掉；Transport 抽象可插拔。
  - 客户端推荐：方案 B（Kotlin Android + Tauri 桌面），次选 KMP；中继用 Go/Spring Boot。
  - 安全：设备配对（SAS/扫码）+ Curve25519/AES-GCM E2E 加密。
  - 留了 5 个待讨论问题（采集方案取舍、是否自建服务器、发布与否、Rust vs KMP、图片文件范围）。
- 用户拍板 5 项决策，文档升级为定稿 v2：无障碍采集（覆盖任意文字复制，透明 Activity 抢焦读剪贴板 + AppOps 兜底）、纯 LAN + SAS 配对防串扰（SPAKE2 + TOFU 公钥 pinning + TLS1.3）、自用离线不上架、桌面端 Tauri 2（内存敏感，排除 Electron）、仅文字（64KB 上限）。
- Android 端 Kotlin + Compose（前台服务 dataSync + NSD），Windows 端 Rust 核心（windows crate 剪贴板监听、mdns-sd、rustls、arboard）。
- 下一步：写 docs/protocol-v1.md（消息格式/配对流程/去重规则），然后搭双端骨架。
