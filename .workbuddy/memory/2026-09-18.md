# 2026-09-18

- ClipBordcast 初始化 git 仓库（main 分支），仓库级提交身份 sothe / sothe@clipbordcast.local（用户全局未配置 git 身份）。
- 完成 Round 1 并提交 c621759：
  - `docs/protocol-v1.md`：协议唯一事实来源（deviceId/Ed25519 身份、mDNS `_clipbordcast._tcp.local.`、SAS+SPAKE2+TOFU 配对、TLS1.3+证书 pinning、长度前缀帧 JSON、CLIP_TEXT/PING/PONG、5s 去重防回环、64KB 截断、日志不落明文）。
  - `windows/`（Tauri 2 + Rust）：clipboard_watcher.rs（HWND_MESSAGE 隐藏窗口 + AddClipboardFormatListener）、discovery.rs（mdns-sd 注册+浏览）、model.rs（消息模型+DedupeWindow）、lib.rs（身份加载、事件 emit 前端）；ui/index.html 用 withGlobalTauri 免打包器。
  - `android/`（Kotlin + Compose）：ClipAccessibilityService（复制事件启发式检测+去抖）、ReadClipboardActivity（透明抢焦读剪贴板）、ClipForegroundService（dataSync 常驻+通知）、NsdHelper（注册 TXT id/name/v/port + 浏览解析）、BootReceiver、MainActivity（无障碍开启引导）。
  - 双端共享 deviceId 存法：Windows %APPDATA%/device_id，Android SharedPreferences("device")。
- Round 2 计划：SAS 配对（SPAKE2）+ TLS 1.3 通信层 + 双端互联联调。
